describe('Service - inputs', () => {
  beforeEach(() => { });

  afterEach(() => {
    jest.resetAllMocks();
  });

  it('checksum', async (done) => {
    expect(1 + 1).toEqual(2);
    done();
  });
});